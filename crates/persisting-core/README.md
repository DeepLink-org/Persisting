# persisting-core

Rust core for Persisting (TAA, block I/O, tiered loop, page fault). Built as the native module `persisting._core` when you run `maturin develop` or `pip install .` from the repo root.

Design: [docs/src/design/tensor_address_algebra.md](../../docs/src/design/tensor_address_algebra.md)
