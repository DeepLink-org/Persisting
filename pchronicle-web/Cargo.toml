[package]
name = "pchronicle-web"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[workspace]

[dependencies]
dioxus = { version = "=0.7.9", features = ["web"] }
dioxus-web = "=0.7.9"
futures-util = "0.3"
gloo-net = { version = "0.6", features = ["http"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
time = { version = "=0.3.55", features = ["parsing"] }
urlencoding = "2"
wasm-bindgen = "0.2"
web-sys = { version = "0.3", features = ["Document", "DomRect", "Element", "History", "HtmlElement", "KeyboardEvent", "Location", "Storage", "UrlSearchParams", "Window"] }
web-time = "=1.1.0"

[profile.dev]
debug = 0
incremental = true

[profile.release]
opt-level = "s"
lto = "thin"
codegen-units = 16
debug = false
